
#include <stdio.h>
#include "libftprintf.h"
#include <string.h>

void ft_stringmanage(va_list arg, t_flags *marker)
{
    char *aux;

    aux = va_arg(arg, int);
    // funcion strlen para saber el tamaño del string y guardarlo en variable, o usarla directamente dentro de otra funcion.

}